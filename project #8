/* 포텐셔미터로 서브모터 위치 조절하기 */

#include <Servo.h>
int potPin = 0;
int srrvoPin = 9:
Servo servo;
Void setup()
{
    servo.attach(servoPin);
}
void loop()
{
    int reading = analogRead(potpin); // 0 to 1023
    // 잘 채워보세요.... 
    servo.write(angle);
}